# Polaroid (frontend) 📸

## Running this application

1. Create a `.env` file based on `.env.example` and set your own environment variables.
2. Install dependencies and start the application:

```sh
npm install
npm start
```